(defun get-divisors (num)
    "Returns the set of divisors of a given number"
    (setq count 2)
    (setq arr [])
    (while (< (sqrt num) count)
        (when (eq 0 (% num count))
            ())))
